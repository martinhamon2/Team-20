spring:
  datasource:
    url: jdbc:h2:mem:icts
    username: icts-team18
    password: icts-team18!
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    properties:
      hibernate.format_sql: true

cors:
  allowed-origins: http://localhost:3000

app:
  base-url: http://localhost:3000

flyway:
  user: icts-team18
  password: icts-team18!
  schemas: public
  url: jdbc:h2:mem:icts
  locations: filesystem:db/migration
  baselineOnMigrate: true
